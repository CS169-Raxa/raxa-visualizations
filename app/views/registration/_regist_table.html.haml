%h3 Registered patients

%table#registered
  %thead
    %tr
      %th Time Completed
      %th Patient
      %th Status
  %tbody
    - @registrations_and_divs.each do |registration, div|
      - if div
        %tr
          %td.divider{:colspan => 3}
            %div
              %span{:title => div}= div
      - end
      %tr
        %td= registration.time_end.strftime('%l:%M %P')
        %td= registration.patient_name
        %td= regisrtation.patient_status
    %tr
      %td.divider{:colspan => 3}
        %div
          -# please change the title attribute of the next line
          %span
            %a{:href => '#'} See more
