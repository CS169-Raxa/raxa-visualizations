= content_for :head do
  = stylesheet_link_tag :departments
  = javascript_include_tag :raxa
  = javascript_include_tag :departments

= content_for :title do
  #{@department.name} statistics

= content_for :subtitle do
  Superuser

= content_for :content do
  %h4 Performance history
  #box_plot{'data-points' => @department_history.to_json}

  -if (@average_time.to_i == 0)
    = "No #{@department.name} history available"
  -else
    = "Average time per #{@department.name} visit: #{@average_time}"
